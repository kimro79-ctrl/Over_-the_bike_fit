name: over_the_bike_fit
description: 자전거 핏 분석 및 실내 워크아웃 추적 앱
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_native_splash: ^2.4.7
  fl_chart: ^1.1.1
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/background.png
    - assets/icons/
